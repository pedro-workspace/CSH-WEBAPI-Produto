# CSH-WEBAPI-Produto
# Atenção ao versionamento dos packages e compatibilidades
<!-- 
EntityFrameworkCore --version 10.0.0
EntityFrameworkCore.Design --version 10.0
dotnet-ef --version 10
Swashbuckle.AspNetCore -->

# API inicial para criação de um modelo de API MVC (Model, View, Controller)

# Adição de configurações de conexão de database e acesso aos arquivos de settings

# Registro do serviço de banco de dados no Program.cs
## Pronto para migrations

<!-- dotnet ef migrations add Migracao_1 
        dotnet ef database update
-->